#include <stdio.h>

int main() {
  int nota;

  printf("Entre com a avaliação do motorista entre 1 e 5:");
  int leu_certo = scanf("%i", &nota);

  switch (nota) {
  case 1() {
    case 2() case (nota == 3) {
      case 3 ()
        case 4 ()
        case 5()
      default:
    printf("Nota invalida\n");
  }
  return 0;
}